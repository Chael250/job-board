# Test Environment Configuration
NODE_ENV=test
PORT=3001

# Test Database - Use a separate test database
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=dever
DB_PASSWORD=password
DB_TEST_NAME=job_board_test

# Redis (for testing)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=1

# JWT (test keys)
JWT_SECRET=test-jwt-secret-key-for-testing-only
JWT_EXPIRES_IN=15m
JWT_REFRESH_SECRET=test-refresh-secret-key-for-testing-only
JWT_REFRESH_EXPIRES_IN=7d

# File Storage (test)
STORAGE_TYPE=local
STORAGE_PATH=./test-uploads
MAX_FILE_SIZE=5242880

# Email (test - no actual sending)
SMTP_HOST=localhost
SMTP_PORT=587
SMTP_USER=test
SMTP_PASS=test
SMTP_FROM=test@jobboard.com

# Security (test)
BCRYPT_ROUNDS=4
RATE_LIMIT_TTL=60000
RATE_LIMIT_MAX=100